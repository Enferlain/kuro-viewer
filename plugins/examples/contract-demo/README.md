# Contract Demo Plugin

This is a minimal example plugin that follows `docs/PLUGIN_CONTRACT_1.0.md`.

Included files:

- `plugin.json` (manifest)
- `settings.schema.json` (declarative settings)
- `frontend.js` (frontend entry stub)

The plugin can be packaged and installed through the normal plugin install flow
to validate manifest handling and plugin list UX.
