# Forensics Suite

Workspace plugin source for Kuro Viewer forensic analysis tools.

This plugin owns:

- settings UI
- analysis hotkeys
- overlay rendering
- score computation

The base app should only interact with it through the generic bundled plugin and analysis-plugin contracts.
